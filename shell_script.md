# Shell Script
 -쉘은 스크립트 언어로 작성된 프로그램을 실행시킬 수 있음

vi hello.sh
```
  #!/bin/bash
  
  # print message
  
  echo "Hello! shell script"
  
  exit 0
```

```
ls -l 
chmod a+x hello.sh
ls -l
./hello.sh
```


